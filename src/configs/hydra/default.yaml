defaults:
  - override hydra_logging: colorlog
  - override job_logging:   colorlog

run:
  dir: logs/${datamodule.params_data.name}/${model.params_model.name}_${datamodule.params_data.task}_${datamodule.params_data.missing_rate}_${tag}

sweep:
  dir: logs/multiruns/${now:%Y-%m-%d}/${now:%H-%M-%S}
  subdir: ${hydra.job.num}_${hydra.job.id}
